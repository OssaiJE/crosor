import React from "react";
import ReactDOM from "react-dom";
import ApolloP from "./ApolloProvider";

ReactDOM.render(
  <React.StrictMode>
    <ApolloP />
  </React.StrictMode>,
  document.getElementById("root")
);
